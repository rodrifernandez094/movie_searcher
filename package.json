{
  "name": "movie-searcher",
  "version": "1.0.0",
  "description": "Node app used to search movie info using the OMDb api.",
  "main": "index.js",
  "scripts": {
    "install-client": "cd client && npm install",
    "build": "cd client && npm run build",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "node index.js",
    "dev": "nodemon index.js"
  },
  "author": "Rodrigo Fernandez",
  "license": "MIT",
  "dependencies": {
    "dotenv": "^8.2.0",
    "ejs": "^3.1.3",
    "express": "^4.17.1",
    "node-fetch": "^2.6.0",
    "path": "^0.12.7"
  },
  "devDependencies": {
    "morgan": "^1.10.0"
  }
}
